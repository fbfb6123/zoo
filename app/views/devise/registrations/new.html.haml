.wrapper
  .single-header-user
    .single-header-user-icon-list
      .single-header__icon-user
        
  .single-main-user
    .single-main-user-form
      .single-main-user-form__title
        会員情報入力
      .single-main-user-form__input
        = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
          = devise_error_messages!
          .field
            .field-label
              = f.label :"お名前(全角)"
              .hissu
                必須
            .field-input
              = f.text_field :name,placeholder: "例）田中 太朗"
            .field-label
              = f.label :"お名前カナ(全角)"
              .hissu
                必須    
            .field-input
              = f.text_field :name_kana,placeholder: "例）タナカ タロウ"
          .field
            .field-label
              = f.label :メールアドレス
              .hissu
                必須
            .field-input
              = f.text_field :email,placeholder: "PC : 携帯どちらでも可"
          .field
            .field-label
              = f.label :電話番号
              .hissu
                必須
            .field-input
              = f.text_field :phone_number,placeholder: '例）09012345678'
          .field
            .field-label
              = f.label :パスワード
              .hissu
                必須
            .field-input
              = f.password_field :password,placeholder: "7文字以上の半角英数字"
          .field
            .field-label
              = f.label :パスワード確認
              .hissu
                必須
            .field-input
              = f.password_field :password_confirmation,placeholder: "7文字以上の半角英数字"
              .field-input-test
                ※英字と数字の両方を含めて設定してください
          .field
            .field-label
              = f.label :住所
              .hissu
                必須
            .field-input
              = f.text_field :city,placeholder: "例）博多区美野島千代1-1-1"
            .field-label
              = f.label :番地・号室
              .hissu
                必須
            .field-input
              = f.text_field :city_adress,placeholder: "例）三井ビル405号"
      
            .field-label
              = f.label :生年月日
              .hissu
                必須
            .single-main-user-form-birthday
              .field-inputs
                = f.date_select :birthday, use_month_numbers: true,start_year: 1930, end_year: (Time.now.year - 10),include_blank: true
            .single-main-user-form-verification__contents-list
              ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
          .actions
            = f.submit "登録する", class: 'btn'
          .single-main-user-form-verification-me
            
  .single-fotter
    %ul.clearfix
      %li.footer-list
        =link_to "#",class:"a" do
          プライバシーポリシー
      %li.footer-list
        =link_to "#",class:"a" do
          利用規約